from pokemon import Pokemon
from pokemontype import GetComparison


def battle(attacker: Pokemon, defender: Pokemon):
    # A1 / D2 + Type1 * Type2 (Type2 = Type2 if type is 0)
    # dmg = battle.Attack
    return ("")
